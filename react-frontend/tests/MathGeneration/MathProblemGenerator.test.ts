/* 
import MathProblemGenerator from "../../src/MathGeneration/MathProblemGenerator"
import { describe, expect, it} from "vitest"

describe('digitsToMax', () => {
  it('returns 1 if number of digits is 0', () => {
    expect(MathProblemGenerator.digitsToMax(3)).toBe(1000)
  })
}) 
*/